# Question

Show the doctor_id, first_name, and last_name of doctors whose first_name ends with 'a'.

# Query

```sql
SELECT doctor_id, first_name, last_name
FROM doctors
WHERE first_name LIKE '%a';
```