# Question

Show patient_id, first_name, and last_name of patients whose gender is 'F' and whose first_name starts with 'A'.

# Query

```sql
SELECT patient_id, first_name, last_name
FROM patients
WHERE gender = 'F'
  AND first_name LIKE 'A%';
```