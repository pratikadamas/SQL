# 26

You can create a MySQL function to calculate a person's age from their date of birth

## Notes
```sql
DELIMITER //
CREATE FUNCTION calculate_age(dob DATE) RETURNS INT
BEGIN
    DECLARE age INT;
    SET age = TIMESTAMPDIFF(YEAR, dob, CURDATE());
    RETURN age;
END //
DELIMITER ;
```


