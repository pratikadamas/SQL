# Question

Show the doctor_id, first_name, and last_name of doctors sorted by last_name in ascending order.

# Query

```sql
SELECT doctor_id, first_name, last_name
FROM doctors
ORDER BY last_name ASC;
```