# 27

Function to Calculate GST (18%)

## Notes
```sql
DELIMITER //
CREATE FUNCTION calculate_gst(amount DECIMAL(10,2)) RETURNS DECIMAL(10,2)
BEGIN
    DECLARE gst DECIMAL(10,2);
    SET gst = amount * 0.18;
    RETURN gst;
END //
DELIMITER ;
```


