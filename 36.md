# Question

Show the patient_id and diagnosis of admissions where the patient_id is greater than 500 and the diagnosis is not null.

# Query

```sql
SELECT patient_id, diagnosis
FROM admissions
WHERE patient_id > 500
  AND diagnosis IS NOT NULL;
```