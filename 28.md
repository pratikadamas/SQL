# 28
voter eligibility criteria in India

## Notes
```sql
DELIMITER //
CREATE FUNCTION is_eligible_to_vote(dob DATE) RETURNS BOOLEAN
BEGIN
    DECLARE age INT;
    SET age = TIMESTAMPDIFF(YEAR, dob, CURDATE());
    RETURN age >= 18;
END //
DELIMITER ;
```


