# Question

Show the patient_id, first_name, and last_name of patients whose last_name contains 'an'.

# Query

```sql
SELECT patient_id, first_name, last_name
FROM patients
WHERE last_name LIKE '%an%';
```