# 29

Email Validation Function

## Notes
```sql
DELIMITER //

CREATE FUNCTION isvalid(e VARCHAR(20))
RETURNS BOOLEAN
DETERMINISTIC
BEGIN
    IF e LIKE '%@%.%' THEN
        RETURN TRUE;
    ELSE
        RETURN FALSE;
    END IF;
END //

DELIMITER ;
```

Add content here.
